spring:
  application:
    name: totalcost_service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/totalcost
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
